jobs:
  include:
  - stage: OSX test
    os: osx
    osx_image: xcode11.3
    language: swift
    script:
    - swift package update
    - swift test --enable-code-coverage
after_success:
  - bash <(curl -s https://codecov.io/bash)
